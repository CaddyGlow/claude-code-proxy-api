"$bindable(): Marks a prop as bindable, allowing data to flow up from child to parent. <FancyInput bind:value={message} />. Can specify fallback value: let { value = $bindable('fallback') } = $props(). $derived(expression): Creates derived reactive state. $derived.by(function): Creates derived state using function. Dependencies: Values read synchronously are dependencies. untrack: Prevents value from being a dependency. $effect: Creates an effect that reacts to state changes. Runs after component is mounted and after DOM updates. Returns a cleanup function that runs before re-running or unmount. $effect(() => { /* effect code */ return () => { /* cleanup */ }; }); $effect.pre: Runs code before DOM updates. $effect.tracking: Returns true if running inside a tracking context. $effect.root: Creates a non-tracked scope, doesn't auto-cleanup. Dependencies: Automatically tracks reactive values ($state, $derived, $props) read synchronously. Async reads are not tracked. $host: Accesses the host element when compiling a component as a custom element. Example: $host().dispatchEvent(new CustomEvent(type)). $inspect(...): Logs value when it changes (dev only). $inspect(...).with(callback): invokes callback instead of console.log. Example: $inspect(count).with((type, count) => { console.log(type, count); }); $props rune accesses component props. Destructuring: `let {prop=default} = $props();` Provides fallback values. Rename props using destructuring. Rest syntax: `let {a,b,...rest} = $props();` Prop updates: Changes in parent component update child component. Child can temporarily override. Don't mutate props directly; use callbacks or $bindable for shared objects. Fallback values are not reactive. Type safety: Add TypeScript or JSDoc types to props for improved clarity. $state(initialValue): Creates reactive state. UI reacts to changes. count is a number. Deep state: arrays/objects are deeply reactive proxies. Classes can use $state in fields. $state.raw(initialValue): State that cannot be mutated, only reassigned. Improves performance. $state.snapshot(proxy): Returns static snapshot of a deeply reactive proxy. animate: Triggered when keyed each block contents are re-ordered. Must be immediate child of keyed each block. <li animate:flip>{item}</li> Parameters: Can have parameters like transitions and actions. Custom animation functions: animation = (node: HTMLElement, { from: DOMRect, to: DOMRect }, params: any) => { delay?: number, duration?: number, easing?: (t: number) => number, css?: (t: number, u: number) => string, tick?: (t: number, u: number) => void } Returns: Object with css or tick method. css preferred. t goes from 0 to 1, u = 1 - t. {#await expression}...{:then name}...{:catch name}...{/await} Await blocks handle Promise states: pending, fulfilled, rejected. Can omit {:catch} or initial block. Example: {#await promise then value} {value} {/await} Basic Markup: Tags: lowercase tags are HTML elements, capitalized tags are components. Attributes: work like HTML attributes. {name} shorthand for name=name. Boolean attributes: included if truthy, excluded if falsy. Component props: Values passed to components. Events: DOM events via on:click, etc. Case sensitive. Event delegation: Svelte uses event delegation for some events (click, change, etc.) Text expressions: {expression}. Use {@html} for rendering HTML. Comments: HTML comments. @component for component documentation. bind: two-way data binding. bind:value, bind:checked, bind:group, bind:files, bind:open, contenteditable bindings, dimensions, bind:this, bind:property. Edge cases: numeric input coercion, FileList manipulation, option value omission, component prop binding. Bindable properties with fallback values. class: Conditionally set classes on elements. Example: . Shorthand: (when name coincides with value). Multiple directives allowed. Compiler errors list various syntax and usage issues in Svelte components, e.g., animation_duplicate, attribute_invalid_name, bind_invalid_target, block_unclosed, const_tag_cycle, css_invalid_selector, each_item_invalid_assignment, element_invalid_closing_tag, event_handler_invalid_modifier, import_svelte_internal_forbidden, legacy_export_invalid, node_invalid_placement, props_duplicate, reactive_declaration_cycle, script_duplicate, slot_attribute_invalid, store_invalid_subscription, transition_conflict, typescript_invalid_feature. Compiler warnings: Indicate potential mistakes. Disable with <!-- svelte-ignore code -->. Examples: a11y_accesskey, a11y_aria_activedescendant_has_tabindex, a11y_aria_attributes, etc. {@const ...} defines a local constant within a block like {#each}, {#if}, or a component. Context: Provides component-scoped state management. setContext(key, value): Associates an object with the current component. getContext(key): Retrieves the context value. hasContext(key): Checks if a context key exists. getAllContexts(): Gets all context keys. Context is not inherently reactive; use $state for reactivity. Custom elements: <svelte:options customElement>: Compiles component to custom element. Component lifecycle: Created/updated in next tick, destroyed in next tick after disconnectedCallback. Component options: tag, shadow, props (attribute, reflect, type), extend. Caveats: Styles encapsulated, not scoped. Not suitable for SSR. Slotted content renders eagerly. let: directive ineffective. Polyfills needed for older browsers. Context not across custom elements. Custom properties (CSS variables) can be passed to components: <Slider --track-color=\"black\" />. Desugars to a wrapper element with style attribute. Can be read inside component with var(--property, fallback). Can be defined on parent element or :root. `{@debug ...}`: logs variable changes, pauses execution in devtools. Accepts comma-separated variable names. `{@debug}` without arguments inserts debugger statement. `{#each ...}`: iterates over arrays, array-like objects, iterables. Includes index: `{#each items as item, i}`. Keyed each blocks for efficient diffing: `{#each items as item (item.id)}`. `{:else}` for empty lists. Destructuring and rest patterns supported. Svelte FAQ: Interactive tutorial for beginners. Support: reference docs, Stack Overflow, Discord, Reddit. Third-party resources: Svelte Society. VS Code syntax highlighting: official extension. Auto-formatting: Prettier with plugin. Component documentation: specially formatted comments. Scalability: check Svelte issue. UI component library: Svelte Society. Testing: unit, component, E2E; Vitest, Playwright, Cypress. Router: SvelteKit, page.js, navaid, others. Unused styles: Svelte removes them; use :global(). Svelte v2: available but not actively maintained. Hot module reloading: SvelteKit, community plugins. Getting Started: Use SvelteKit (npx sv create myapp) or Svelte with Vite (npm create vite@latest). VS Code extension available. `:global(...)`: applies styles globally. `:global {...}`: applies styles to multiple selectors globally. Global `@keyframes`: prepend with `-global-`. {@html: inject raw HTML; prevent XSS; styles require :global(). {#if expression}...{/if} Conditionally render content. {:else if expression}...{/if}. {:else}...{/if}. Example: {#if answer === 42} <p>question?</p> {/if} Imperative API: mount(component, options): Mounts component. unmount(component): Unmounts component. render(component, options): Server-side rendering. hydrate(component, options): Hydrates component. in: and out: Directives similar to transition:, but not bidirectional. In transitions play alongside out. Out transitions restart if aborted. Example: <div in:fly={{ y: 200 }} out:fade></div> {#key expression}...{/key} Recreates contents when expression changes. Used for component reinstantiation or transitions. {#key value} {/key} $$props: Contains all props passed to a component. $$restProps: Contains props excluding those declared with export. Usage: <button {...$$restProps} class=\"variant-{variant} {$$props.class ?? ''}\">. `$$slots`: object with slot names as keys in legacy mode. Check `$$slots` object for slot content. Example: `{#if $$slots.description}`. Imperative component API. const component = new Component(options). Options: target: HTMLElement, anchor: HTMLElement child of target, props: {}, context: Map, hydrate: boolean, intro: boolean. $set(props): sets props. $on(ev, callback): listen to events, returns off function. $destroy(): Removes component. Component.render(props, options): Server-side render, returns {head, html, css}. export let: Marks a component prop. export let foo; export let bar = 'default value'; Exported const/class/function are part of component API. Can rename props: export { className as class }; Reactive let/var (legacy mode): Variables at the top level are reactive. Assignment triggers updates. Array methods like .push() require re-assignment to trigger UI updates. Example: let count = 0; <button on:click={() => count += 1}> on: (legacy mode) Event handlers. <button on:click={handler}>. Inline handlers: <button on:click={() => ...}>. Modifiers: preventDefault, stopPropagation, stopImmediatePropagation, passive, nonpassive, capture, once, self, trusted. Forward events with <button on:click>. Multiple listeners allowed. Component events: createEventDispatcher(). dispatch(event, detail). Use callback props instead for Svelte 5 migration. Svelte 5 API changes: runes, snippets, event attributes. Legacy (Svelte 3/4) features deprecated. Migrate incrementally. Legacy and runes modes distinguished. Runes mode disables legacy features. Reactive $: statements: $: statement. Dependencies determined at compile time. Example: $: sum = a + b;. $: if (browser) { ... } for browser-only code. <slot>: Renders slotted content. Named slots: <slot name=\"...\">. Fallback content: Content inside <slot>. Passing data: let: directive. Named slots data: let: directive on slot attribute element. <svelte:component this={component}>: Dynamically renders components in legacy mode. : Place content in a named slot without a wrapping DOM element. Obsolete in Svelte 5+. Recursively include a component. Must be inside if/each or slot to prevent loops. Obsolete: import the component directly instead. Lifecycle hooks: onMount(callback: () => any): Schedules callback after component is mounted. Return function from callback to run on unmount. onDestroy(callback: () => any): Schedules callback before component is unmounted. tick(): Promise that resolves after state changes. Nested <style> elements: Inserted as-is into the DOM without scoping or processing. Svelte is a framework for building user interfaces. It uses a compiler to create optimized JavaScript. Use it to build components and full stack apps. `{@render ...}`: renders snippets. Example: `{@render sum(1,2)}`. Optional chaining for undefined snippets: `{@render children?.()}`. Use `{#if ...}` for fallback content. bind_invalid_checkbox_value: Use `bind:checked` for checkboxes. bind_invalid_export: Use `bind:this` and access properties. bind_not_bindable: Mark property as bindable: `let { key = $bindable() } = $props()`. component_api_changed: Svelte 5 component API change. component_api_invalid_new: Use compatibility.componentApi: 4 for Svelte 4 components. derived_references_self: Recursive derived value reference disallowed. each_key_duplicate: Duplicate keys in keyed each block. effect_in_teardown: Runes cannot be used in effect cleanup. effect_in_unowned_derived: Effect creation inside unowned derived value disallowed. effect_orphan: Runes only usable inside effects. effect_update_depth_exceeded: Maximum update depth exceeded. hydration_failed: Hydration failure. invalid_snippet: Use optional chaining for null/undefined snippets. lifecycle_legacy_only: Legacy lifecycle function not in runes mode. props_invalid_value: Cannot bind undefined with fallback value. props_rest_readonly: Rest properties are readonly. rune_outside_svelte: Runes only in .svelte files. state_descriptors_fixed: Fixed property descriptors on $state. state_prototype_fixed: Cannot set prototype of $state. state_unsafe_local_read: Reading locally created state inside derived forbidden. state_unsafe_mutation: Updating state inside derived/template forbidden. lifecycle_function_unavailable: Server-side lifecycle function unavailable. invalid_default_snippet: Cannot use {@render children(...)} with let: directives. lifecycle_outside_component: Lifecycle function only during initialization. store_invalid_shape: Invalid store shape. svelte_element_invalid_this_value: this prop must be a string. Runtime warnings: binding_property_non_reactive: Binding to non-reactive property. console_log_state: Use $inspect or $state.snapshot for logging $state proxies. event_handler_invalid: Handler must be a function. hydration_attribute_changed: Attribute value mismatch between server and client renders. hydration_html_changed: {@html} block value mismatch. hydration_mismatch: Hydration failed due to UI mismatch. invalid_raw_snippet_render: render function must return single element HTML. legacy_recursive_reactive_block: Recursive updates detected in migrated reactive block. lifecycle_double_unmount: Tried unmounting unmounted component. ownership_invalid_binding: Invalid binding ownership. ownership_invalid_mutation: Mutating external values discouraged. state_proxy_equality_mismatch: $state proxy equality comparison issues. dynamic_void_element_content: Void element cannot have content. state_snapshot_uncloneable: Value cannot be cloned with $state.snapshot Scoped styles in Svelte components are enabled with the <style> element. CSS is scoped by default to the component. Uses a class based on a hash (e.g. svelte-123xyz). Selectors receive a specificity increase of 0-1-0. Keyframe names are also scoped. {#snippet name(param1, param2, paramN)}...{/snippet} Creates reusable markup chunks. Can have parameters with default values. Can reference values outside themselves. Can reference themselves and each other. Can be passed to components as props: <Table data={fruits} {header} {row} /> Snippets inside a component implicitly become props. Content inside component tags becomes part of the children snippet. Snippets implement the Snippet interface. Can be created programmatically with createRawSnippet. Stores enable reactive access to a value. Access value with $. Declared at top level. svelte/store provides store implementations. writable(initialValue, start): Creates a store with set(value) and update(callback). start function called on first subscription, returns stop function. readable(initialValue, start): Value cannot be set from outside. derived(stores, callback, initialValue): Derives a store from other stores. Callback takes store values and set/update functions. readonly(store): Makes a store readonly. get(store): Returns store value (not recommended in hot code paths). Store contract: store = { subscribe: (subscription) => (() => void), set?: (value) => void } Must have subscribe, may have set. style: directive for setting multiple styles. Supports expressions and |important modifier. Directives take precedence over style attributes. afterUpdate(fn: () => void): void; Schedules a callback to run after component update. beforeUpdate(fn: () => void): void; Schedules a callback to run before component update. createEventDispatcher = any>(): EventDispatcher; Creates an event dispatcher. dispatch(name: string, detail?: any): void; createRawSnippet(fn: (...params: Getters) => { render: () => string; setup?: (element: Element) => void | (() => void); }): Snippet; Create snippet programmatically. flushSync(fn?: (() => void) | undefined): void; Synchronously flushes state changes. getAllContexts = Map>(): T; Retrieves the context map. getContext(key: any): T; Retrieves context by key. hasContext(key: any): boolean; Checks if a key exists in context. hydrate, Exports extends Record>(component: ComponentType> | Component, options: { target: Document | Element | ShadowRoot; props?: Props; events?: Record any>; context?: Map; intro?: boolean; recover?: boolean; }): Exports; Hydrates a component. mount, Exports extends Record>(component: ComponentType> | Component, options: MountOptions): Exports; Mounts a component. onDestroy(fn: () => any): void; Schedules a callback before unmount. onMount(fn: () => NotFunction | Promise> | (() => any)): void; Schedules a callback after mount. setContext(key: any, context: T): T; Associates a context object. tick(): Promise; Resolves after state changes. unmount(component: Record): void; Unmounts a component. untrack(fn: () => T): T; Excludes state read inside fn from dependency tracking. Component interface Component = {}, Exports extends Record = {}, Bindings extends keyof Props | '' = string> { } ComponentConstructorOptions interface ComponentConstructorOptions = Record> { target: Element | Document | ShadowRoot; anchor?: Element; props?: Props; context?: Map; hydrate?: boolean; intro?: boolean; recover?: boolean; sync?: boolean; $$inline?: boolean; } ComponentProps type ComponentProps> = Comp extends SvelteComponent ? Props : Comp extends Component ? Props : never; ComponentType type ComponentType = (new (options: ComponentConstructorOptions ? Props : Record>) => Comp) & { element?: typeof HTMLElement; }; EventDispatcher interface EventDispatcher> { ( ...args: null extends EventMap[Type] ? [type: Type, parameter?: EventMap[Type] | null | undefined, options?: DispatchOptions] : undefined extends EventMap[Type] ? [type: Type, parameter?: EventMap[Type] | null | undefined, options?: DispatchOptions] : [type: Type, parameter: EventMap[Type], options?: DispatchOptions] ): boolean; } MountOptions type MountOptions = Record> = { target: Document | Element | ShadowRoot; anchor?: Node; events?: Record any>; context?: Map; intro?: boolean; } & ({} extends Props ? { props?: Props; } : { props: Props; }); Snippet interface Snippet { } SvelteComponent class SvelteComponent = Record, Events extends Record = any, Slots extends Record = any> { } SvelteComponentTyped class SvelteComponentTyped = Record, Events extends Record = any, Slots extends Record = any> extends SvelteComponent { } svelte/action: type Action = (node: Element, parameter?: any) => ActionReturn. ActionReturn {update?: (parameter: any) => void, destroy?: () => void}. Actions are called when an element is created. Example: export const myAction: Action = (node, param) => { return { destroy() {} } } svelte/animate: flip(node: Element, { from: DOMRect; to: DOMRect; }, params?: FlipParams): AnimationConfig. Animates element's x and y translation. FlipParams: delay?: number; duration?: number | ((len: number) => number); easing?: (t: number) => number. <svelte:body onevent={handler} use:someAction /> Allows adding listeners/actions to document.body. Can only appear at the top level of the component and must never be inside a block or element. VERSION: string - current version. compile(source: string, options: CompileOptions): CompileResult - compiles .svelte to JS module. compileModule(source: string, options: ModuleCompileOptions): CompileResult - compiles JS with runes to JS module. migrate(source: string, options?: {filename?: string; use_ts?: boolean}): {code: string} - migrates Svelte code to runes. May throw errors. parse(source: string, options?: {filename?: string; modern?: boolean}): AST.Root | Record - parses component to AST. `modern` option for AST version. preprocess(source: string, preprocessor: PreprocessorGroup|PreprocessorGroup[], options?: {filename?: string}): Promise<Processed> - transforms component source code. walk(): never - estree-walker import replacement. CompileOptions: Includes ModuleCompileOptions, name, customElement, accessors, namespace, immutable, css, cssHash, preserveComments, preserveWhitespace, runes, discloseVersion, compatibility, componentApi, sourcemap, outputFilename, cssOutputFilename, hmr, modernAst. ModuleCompileOptions: dev, generate, filename, rootDir, warningFilter. CompileResult: js, code, map, css, warnings, metadata, runes, ast. CompileError: extends ICompileDiagnostic. MarkupPreprocessor: (options: {content: string; filename?: string})=>Processed|void|Promise<Processed|void> Preprocessor: (options: {content: string; attributes: Record<string, string|boolean>; markup: string; filename?: string})=>Processed|void|Promise<Processed|void> PreprocessorGroup: name, markup, style, script. Processed: code, map, dependencies, attributes, toString. Warning: extends ICompileDiagnostic. AST: BaseNode, Fragment, Root, SvelteOptions, Text, ExpressionTag, HtmlTag, Comment, ConstTag, DebugTag, RenderTag, AnimateDirective, BindDirective, ClassDirective, LetDirective, OnDirective, StyleDirective, TransitionDirective, UseDirective, BaseElement, Component, TitleElement, SlotElement, RegularElement, SvelteBody, SvelteComponent, SvelteDocument, SvelteElement, SvelteFragment, SvelteHead, SvelteOptionsRaw, SvelteSelf, SvelteWindow, EachBlock, IfBlock, AwaitBlock, KeyBlock, SnippetBlock, Attribute, SpreadAttribute, Script. <svelte:document> allows access to the document object. onevent={handler}: Adds event listener to document. bind:prop={value}: Binds to document properties (activeElement, fullscreenElement, pointerLockElement, visibilityState; all readonly). use:someAction: Uses action on document. Must be at the top level of a component. svelte/easing: Easing functions for transitions/animations. import { linear, quadIn, ... } from 'svelte/easing'; Each function takes t:number, returns number. Functions: backIn(t), backInOut(t), backOut(t), bounceIn(t), bounceInOut(t), bounceOut(t), circIn(t), circInOut(t), circOut(t), cubicIn(t), cubicInOut(t), cubicOut(t), elasticIn(t), elasticInOut(t), elasticOut(t), expoIn(t), expoInOut(t), expoOut(t), linear(t), quadIn(t), quadInOut(t), quadOut(t), quartIn(t), quartInOut(t), quartOut(t), quintIn(t), quintInOut(t), quintOut(t), sineIn(t), sineInOut(t), sineOut(t). ``: renders unknown elements. Only `bind:this` supported. Nullish `this` prevents rendering. Void element with children throws error. Use `xmlns` for explicit namespaces. `this` must be a valid DOM element tag. on(window: Window, type: Type, handler: (this: Window, event: WindowEventMap[Type]) => any, options?: AddEventListenerOptions | undefined): () => void; Attaches an event handler to the window, returns a function to remove handler. Also defined for Document, HTMLElement, MediaQueryList, EventTarget .svelte files: <script>: Instance-level logic. <script module>: Module-level logic, exports bindings. <style>: Scoped styles. inserts elements into document.head. Example: <svelte:head><title>Hello world!</title></svelte:head> .svelte.js/.svelte.ts files: JS/TS modules that support runes, useful for reusable reactive logic. `svelte/legacy`: functions for migration. `asClassComponent`, `createBubbler`, `createClassComponent`, `handlers`, `nonpassive`, `once`, `passive`, `preventDefault`, `run`, `self`, `stopImmediatePropagation`, `stopPropagation`, `trusted`. All imports deprecated. svelte/motion: spring: animated store simulating spring behavior. tweened: store with smooth transitions. Spring interface: set, update, precision, damping, stiffness. Tweened interface: set, update. Specify per-component compiler options: runes={true|false}, namespace=\"...\", customElement={...}, css=\"injected\". Deprecated options: immutable={true|false}, accessors={true|false}. Example: svelte/reactivity: Reactive built-ins: SvelteDate, SvelteMap, SvelteSet, SvelteURL, SvelteURLSearchParams. SvelteMap: set(key, value):this. SvelteSet: add(value):this. SvelteURL: searchParams: SvelteURLSearchParams. SvelteURLSearchParams: [REPLACE](params: URLSearchParams): void. svelte/server: render(component: ComponentType, options?: { props?: Props; context?: Map }): RenderOutput. Renders a component server-side, returning body and head properties. Only available on the server. svelte/store: derived(stores, fn): Creates derived store. fromStore(store): Gets store value. get(store): Gets current store value. readable(value, start): Creates readable store. readonly(store): Creates readonly store. toStore(get, set): Creates writable store from functions. writable(value, start): Creates writable store. Readable: subscribe(run, invalidate): Subscribes to store updates. StartStopNotifier: (set, update) => () => void. Subscriber: (value) => void. Unsubscriber: () => void. Updater: (value) => T. Writable: set(value), update(updater): Updates and subscribes to store. blur(node: Element, { delay, duration, easing, amount, opacity }?: BlurParams | undefined): TransitionConfig; Animates a blur filter and opacity. crossfade({ fallback, ...defaults }: CrossfadeParams & { fallback?: (node: Element, params: CrossfadeParams, intro: boolean) => TransitionConfig; }): [(node: any, params: CrossfadeParams & { key: any; }) => () => TransitionConfig, (node: any, params: CrossfadeParams & { key: any; }) => () => TransitionConfig]; Creates send/receive transitions, transforms and fades elements. draw(node: SVGElement & { getTotalLength(): number; }, { delay, speed, duration, easing }?: DrawParams | undefined): TransitionConfig; Animates the stroke of an SVG element. fade(node: Element, { delay, duration, easing }?: FadeParams | undefined): TransitionConfig; Animates the opacity of an element. fly(node: Element, { delay, duration, easing, x, y, opacity }?: FlyParams | undefined): TransitionConfig; Animates x/y positions and opacity. scale(node: Element, { delay, duration, easing, start, opacity }?: ScaleParams | undefined): TransitionConfig; Animates opacity and scale. slide(node: Element, { delay, duration, easing, axis }?: SlideParams | undefined): TransitionConfig; Slides an element in and out. TransitionConfig interface TransitionConfig { delay?: number; duration?: number; easing?: EasingFunction; css?: (t: number, u: number) => string; tick?: (t: number, u: number) => void; } BlurParams, CrossfadeParams, DrawParams, FadeParams, FlyParams, ScaleParams, SlideParams interfaces specify parameters for each function. allows adding event listeners to the window object. It can bind to properties like innerWidth, innerHeight, scrollX, scrollY, online, and devicePixelRatio. Example: <svelte:window onkeydown={handleKeydown} bind:scrollY={y} /> Testing: Vitest for unit/integration tests (requires jsdom); Playwright for E2E tests. Component testing: mount, unmount, @testing-library/svelte. Runes usage in tests. transition: directive for transitions. transition:fade. transition:fade|global. transition:fade={{ duration: 2000 }}. Custom transition: transition = (node, params, options:{direction}) => { delay?: number, duration?: number, easing?: (t: number) => number, css?: (t: number, u: number) => string, tick?: (t: number, u: number) => void }. Events: introstart, introend, outrostart, outroend. TypeScript support: `lang=\"ts\"` in ` use: Actions are functions called when an element is mounted using the `use:` directive. function myaction(node, data) { $effect(() => { /* setup */ return () => { /* teardown */ }; }); } <div use:myaction={data}></div> Action: node is the element, data is optional argument. Typing: Action<HTMLDivElement, null, { onswiperight: (e: CustomEvent) => void; }> Runes are symbols (keywords) with a $ prefix used in .svelte and .svelte.js/.svelte.ts files to control the Svelte compiler. Example: let message = $state('hello'); They are part of the language, not values, and valid only in certain positions."
